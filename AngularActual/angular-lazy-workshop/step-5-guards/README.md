# step-5-guards

Adds mock Auth service + auth guard + admin route (lazy)

Run:

1. Node >= 20
2. npm install
3. npm start


Notes:
- Click 'Login' in the header to set the mock auth state, then visit /admin. Click 'Logout' to clear it.
